import { Box } from "@mantine/core";
import TravelPlanTimeline from "@/components/blocks/ui/TravelPlanTimeline"
export default function TraceTimeline({ runs,parentId,firstDate }) {
  return <Box>ََََََََََََََََََََََِِِِِِِِِِِِِِِِ<TravelPlanTimeline runs={runs} parentId={parentId} firstDate={firstDate}/></Box>
}